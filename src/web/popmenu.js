function newPopMenu(title, list, token, methode, action) {
    var popMenu = document.createElement("div");
    popMenu.innerHTML = `
        <div>Text ?</div>
        <input type="hidden" name="token" value="`+token+`">
    `;
    //<input name="" type="" value="" placeholder="" class="inputpopmenu">
}